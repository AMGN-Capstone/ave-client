# UI 색상 테마 현황 및 변경 요청 기준

## 문서 목적

이 문서는 현재 AVE 웹 UI에 실제로 적용된 색상과 용도를 정리한 기준표다. UI 색상 변경 요청을 받을 때 이 표의 **역할명**을 기준으로 변경 범위와 결과를 합의한다.

테마의 기준 파일은 `ui/src/WorkflowApp.css`이며, 실제 화면은 `ui/src/WorkflowApp.tsx`가 렌더링한다. `static/ui`는 빌드 결과물이므로 직접 수정하지 않는다.

## 현재 테마 선택 방식

| 항목 | 현재 구현 |
| --- | --- |
| 선택값 | `localStorage`의 `ave-theme` |
| 적용 위치 | `<html data-theme="light">` 또는 `<html data-theme="dark">` |
| 전환 제어 | 헤더의 테마 전환 버튼 |
| 색상 정의 | `WorkflowApp.css`의 `:root` 및 `:root[data-theme="dark"]` |

## 현재 적용 색상

### 공통 역할 색상

| 역할명 | CSS 변수 | 라이트 테마 | 다크 테마 | 실제 사용 영역 |
| --- | --- | --- | --- | --- |
| 페이지 배경 | `--ave-bg` | `#f1f5f9` | `#111827` | 브라우저 배경, 그래프 배경, 선택 전 단계의 작업 영역 |
| 표면 | `--ave-surface` | `#ffffff` | `#1f2937` | 헤더, 워크플로우 패널, 입력창, 진행률 도크 |
| 기본 글자 | `--ave-ink` | `#172033` | `#f1f5f9` | 제목, 본문, 아이콘, 테두리 버튼의 글자 |
| 보조 글자 | `--ave-muted` | `#5f6b7a` | `#cbd5e1` | 설명문, 보조 안내, 시간 축 레이블, 진행 상태 문구 |
| 구분선 | `--ave-line` | `#d7e0ea` | `#475569` | 패널·입력·표·진행률 바탕·탭 버튼의 테두리와 그래프 그리드 |
| 주요 강조 | `--ave-accent` | `#f97316` | `#fb923c` | 주요 버튼, 활성 탭, 진행률, 히트맵 막대, 단계 번호, 선택 상태 |
| 주요 강조 호버 | `--ave-accent-hover` | `#fb923c` | `#fdba74` | 주요 주황색 버튼과 다운로드 링크의 호버 배경 |
| 강조 위 글자 | `--ave-on-accent` | `#ffffff` | `#172033` | 주요 버튼과 단계 번호의 글자 |
| 약한 강조 배경 | `--ave-soft` | `#fff7ed` | `#7c2d12` | 로그인 상태 배지, 선택된 편집 구간의 배경 |
| 보조 강조 | `--ave-secondary` | `#2563eb` | `#60a5fa` | 보조 버튼·테마 버튼의 호버 테두리와 글자 |
| 보조 강조 호버 | `--ave-secondary-hover` | `#3b82f6` | `#93c5fd` | 파란색 활성 메타데이터 탭의 호버 배경 |
| 연한 보조 강조 배경 | `--ave-secondary-soft` | `#eff6ff` | `#1e3a8a` | 보조 버튼·테마 버튼의 호버 하이라이트 |
| 보조 강조 배지 배경 | `--ave-secondary-badge` | `#dbeafe` | `#1e40af` | 3단계 점수·채팅 밀도 배지 배경 |
| 히트맵 막대 | `--ave-heatmap` | `#2563eb` | `#60a5fa` | 히트맵 그래프 데이터 막대 |
| 드롭다운 호버 항목 | `--ave-select-selected` | `#e5e7eb` | `#e5e7eb` | React 커스텀 드롭다운 목록에서 마우스를 올린 항목의 배경 |
| 드롭다운 호버 글자 | `--ave-select-selected-ink` | `#172033` | `#172033` | 연한 회색 호버 배경 위의 글자 |
| 폼 포커스 | `--ave-focus` | `#2563eb` | `#60a5fa` | 클릭 또는 키보드로 활성화된 입력·선택·텍스트 영역의 파란 테두리 |

### 고정 사용 색상

다음 색상은 현재 일부 요소에 직접 사용된다. 이들은 테마 변수로 아직 분리되지 않았으므로, 변경 요청 시 별도 확인이 필요하다.

| 색상 | 사용 위치 | 비고 |
| --- | --- | --- |
| `#000000` | 동영상 미리보기 영역의 배경 | 영상 영역의 기본값이며 테마와 분리 |
| `#0f172a0d` | 워크플로우 패널 그림자 | 아주 약한 반투명 그림자 |

## 화면별 색상 적용표

| 화면 요소 | 기본 상태 | 선택/강조 상태 | 변경 요청 시 지정할 역할 |
| --- | --- | --- | --- |
| 페이지 전체 | 페이지 배경 | - | 페이지 배경 |
| 헤더·패널·도크 | 표면, 구분선 | - | 표면 / 구분선 |
| 주요 버튼 | 주요 강조 배경, 흰 글자 | 활성 탭도 같은 강조색 | 주요 강조 / 강조 위 글자 |
| 보조 버튼·테마 버튼 | 투명 배경, 기본 글자, 구분선 | - | 기본 글자 / 구분선 |
| 입력창·선택창 | 표면, 기본 글자, 구분선 | 포커스 시 단일 파란 테두리 | 폼 포커스 |
| 드롭다운 목록 항목 | 표면, 기본 글자 | 호버 항목만 연한 회색 배경 | 드롭다운 호버 항목 |
| 상태 배지·선택 구간 | 약한 강조 배경, 주요 강조 글자 | - | 약한 강조 배경 / 주요 강조 |
| 히트맵 | 페이지 배경, 구분선 그리드 | 파란색 데이터 막대 | 페이지 배경 / 구분선 / 히트맵 막대 |
| 메타데이터 탭 | 투명 배경, 기본 글자, 구분선 | 활성 탭은 파란색 배경, 호버 시 밝은 파란색 | 보조 강조 / 보조 강조 호버 / 강조 위 글자 |
| 2단계 체크박스 | 표면, 구분선 | 체크 상태는 파란색 | 보조 강조 |
| 3단계 챕터·구간 선택 | 표면, 구분선 | 선택된 체크박스·구간 카드는 연한 파란색, 점수·채팅 밀도 배지는 더 진한 파란 배경 | 보조 강조 / 연한 보조 강조 배경 / 보조 강조 배지 배경 |
| 설명·시간·보조 정보 | 보조 글자 | - | 보조 글자 |
| 주요 주황색 버튼 호버 | 밝은 주황색 배경, 현재 위치 유지 | 주황색 계열 외곽 하이라이트 | 주요 강조 호버 / 약한 강조 배경 |
| 무색 보조 버튼·테마 버튼 호버 | 투명 배경, 현재 위치 유지 | 파란색 테두리와 외곽 하이라이트 | 보조 강조 / 연한 보조 강조 배경 |

## 테마 변경 요청 방법

색상 변경을 요청할 때 아래 형식으로 알려주면, 영향을 받는 UI를 빠짐없이 수정할 수 있다.

```text
- 변경할 역할: 주요 강조
- 현재 느낌 또는 색상: 밝은 주황색 (#f97316 / 다크 #fb923c)
- 원하는 방향: 더 푸른 계열의 차분한 색상
- 적용 범위: 주요 버튼, 활성 탭, 진행률, 히트맵 막대 모두
- 유지할 요소: 선택 구간의 연한 배경은 현재대로 유지
```

원하는 결과를 색상 코드 대신 표현으로 전달해도 된다. 예를 들면 다음과 같다.

- “배경을 더 따뜻한 회색으로 바꿔 주세요.” → 페이지 배경과 표면의 후보 색상을 제안
- “버튼과 히트맵을 서로 다른 색으로 분리해 주세요.” → 주요 강조와 히트맵 전용 강조 역할을 분리
- “다크 모드에서 글자가 너무 밝습니다.” → 기본 글자와 보조 글자만 조정
- “경고와 오류를 눈에 띄게 표시해 주세요.” → 현재 없는 경고·오류 상태색을 추가하고 해당 상태 UI까지 연결
- “보조 버튼의 파란색을 더 차분하게 바꿔 주세요.” → 보조 강조와 연한 보조 강조 배경만 변경

## 변경 처리 기준

1. 같은 역할의 색상은 라이트와 다크 테마에서 함께 변경한다.
2. “버튼 색상”처럼 화면 요소를 지칭한 요청은 위 화면별 적용표에서 연결된 역할 전체에 적용할지 먼저 확인한다.
3. 일부 화면만 다른 색이 필요한 경우, 기존 역할을 덮어쓰지 않고 해당 목적의 새 역할을 추가한다. 예: 히트맵만 다른 색이 필요하면 `--ave-heatmap`을 추가한다.
4. 색상 변경 후 라이트/다크 양쪽에서 텍스트, 테두리, 선택 상태, 진행률, 히트맵을 확인한다.
5. 변경은 반드시 `ui/src`에 적용하고, 이후 `ui` 디렉터리에서 빌드한다.
6. 호버 효과는 `:disabled`가 아닌 버튼과 다운로드 링크에만 적용한다. 주요 주황색 버튼은 주황색, 보조 버튼과 테마 버튼은 파란색 계열로 하이라이트한다. 비활성화 버튼에는 이동·하이라이트 효과를 적용하지 않는다.
7. 입력창·선택창·텍스트 영역의 포커스 표시는 단일 파란 테두리로 통일한다. 브라우저 기본 포커스 색상이나 이중 외곽선에 의존하지 않는다.
8. Phase 2의 선택 메뉴는 운영체제 기본 드롭다운이 아닌 React 커스텀 메뉴를 사용한다. 마우스를 올린 항목만 `--ave-select-selected` 연한 회색으로 표시하고, 포인터가 벗어나면 즉시 해제한다.

## 현재 미정의 상태색

완료·경고·오류·정보 전용 색상은 아직 공통 테마 변수로 정의되어 있지 않다. 이 상태들을 별도 색상으로 구분하길 원할 때 다음 역할을 추가한다.

| 추가 역할 | 예정 용도 |
| --- | --- |
| 성공 | 완료 상태와 성공 알림 |
| 경고 | 확인이 필요한 안내 |
| 오류 | URL 검증 실패, API 실패 등 |
| 정보 | 중립적 시스템 안내 |
| 포커스 | 키보드 탐색 시 입력·버튼 강조 |

빌드 및 소스 관리 원칙은 [프론트엔드 빌드 원칙](FRONTEND_BUILD_PRINCIPLES.md)을, 화면 크기 기준은 [반응형 디자인 기준](RESPONSIVE_DESIGN.md)을 따른다.
