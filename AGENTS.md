# AGENTS.md

## 역할

`ave-client`는 AVE의 클라이언트 모듈이다.

주요 역할:

* 로컬 서버 실행
* 웹 기반 사용자 인터페이스 제공
* `ave-server`와 통신
* 영상 처리 요청 및 결과 표시

## 실행 구조

* 클라이언트 엔진은 로컬 FastAPI 서버로 동작한다.
* 로컬 서버는 백그라운드에서 실행되며 시스템 트레이에 유지된다.
* 사용자 인터페이스는 별도의 데스크톱 창이 아니라 웹브라우저를 통해 접속한다.
* 시스템 트레이 메뉴에서는 다음 기능을 제공한다.
  * 웹 UI 열기
  * 서버 로그 CMD 열기
  * 프로그램 종료

## 기술 스택

### 클라이언트 엔진

* Python
* FastAPI
* yt-dlp 바이너리
* FFmpeg 바이너리
* SQLite 로컬 작업 DB

### 클라이언트 UI

* React
* TypeScript
* Tailwind CSS
* Vite

## 개발 원칙

* 현재 요구사항에 필요한 최소 기능부터 구현한다.
* 아직 정해지지 않은 구조나 기술은 임의로 확정하지 않는다.
* 변경하기 쉬운 단순한 구조를 우선한다.
* `ave-server`와의 인터페이스 변경 시 관련 문서를 함께 확인한다.

## 작업 범위

이 저장소에서는 클라이언트 관련 코드만 관리한다.

서버 처리 로직, STT 백엔드 구현, 배포 전용 로직은 다른 모듈에서 관리한다.
## 개발 단계 작업물

테스트와 개발 실행으로 생성된 `media/` 파일과 로컬 SQLite DB는 보존 대상이 아니다. 이전 파일 형식이나 작업 결과를 위한 호환 코드·마이그레이션을 추가하지 말고, 구조 변경 후 새 작업으로 검증한다.
